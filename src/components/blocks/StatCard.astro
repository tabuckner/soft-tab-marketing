---
interface Props {
  value: string;
  label: string;
}

const { value, label } = Astro.props;
---

<div class="text-center py-4">
  <p class="text-5xl md:text-6xl font-bold text-gradient-brand leading-none">{value}</p>
  <p class="mt-3 text-sm text-secondary tracking-wide">{label}</p>
</div>
