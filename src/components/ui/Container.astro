---
interface Props {
  class?: string;
  narrow?: boolean;
}

const { class: className = '', narrow = false } = Astro.props;
const widthClass = narrow ? 'max-w-3xl' : 'max-w-7xl';
---

<div class:list={['mx-auto px-4 sm:px-6 lg:px-8', widthClass, className]}>
  <slot />
</div>
